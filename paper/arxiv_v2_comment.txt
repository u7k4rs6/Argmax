v2: the mechanism claim of Section 4.4 is revised, not extended. v1 attributed
both gate failures to confidence not tracking correctness; a third signal, the
answer-token log-probability margin, shows the entropy gate was reading a
diluted statistic and the margin fails on saturation instead, while the
agreement gate's failure remains unexplained. All v1 results, tables and
pre-registered verdicts are unchanged. Adds the margin result, a serverless
reasoning-model evaluation wall, a registered replication the paper's own
comparability rule rejected, and a corrections section. 18 pages.
